# samfedbiz.com Environment Configuration
# Copy to .env for local development
# In production, set these in Hostinger hPanel Environment Variables

# Application Environment
APP_ENV=development
TIMEZONE=Asia/Dubai

# AI Services (NEVER expose client-side)
OPENAI_API_KEY=__SET_IN_HPANEL__
GEMINI_API_KEY=__SET_IN_HPANEL__

# Google OAuth & APIs (NEVER expose client secret client-side)
GOOGLE_CLIENT_ID=__SET_IN_HPANEL__
GOOGLE_CLIENT_SECRET=__SET_IN_HPANEL__
GOOGLE_REDIRECT_URI=https://samfedbiz.com/oauth/google/callback

# SMTP Configuration (NEVER expose password client-side)
SMTP_HOST=__SET_IN_HPANEL__
SMTP_USER=__SET_IN_HPANEL__
SMTP_PASS=__SET_IN_HPANEL__

# Database (Optional - may use Hostinger defaults)
DB_HOST=localhost
DB_NAME=samfedbiz
DB_USER=__SET_IN_HPANEL__
DB_PASSWORD=__SET_IN_HPANEL__

# Optional Features
TELEGRAM_BOT_TOKEN=__OPTIONAL__
RATE_LIMIT_PER_HOUR=100

# Security Notes:
# - NEVER commit .env file to repository
# - Set production values in Hostinger hPanel only
# - All sensitive keys must be server-side only
# - Use EnvManager::getMasked() for display